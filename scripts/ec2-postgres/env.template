# PostgreSQL Database Configuration for EC2
# Copy this file to ~/portfolio-db/.env on EC2 and replace <replace-with-secure-password> with actual password
# DO NOT commit the actual .env file to git

POSTGRES_DB=edens_zac
POSTGRES_USER=zedens
POSTGRES_PASSWORD=<replace-with-secure-password>
